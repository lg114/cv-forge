---
name: cv-forge
description: 轻量简历优化。在不虚构、不夸大前提下，基于可追溯的事实账本改进结构、措辞与岗位针对性；默认输出 Markdown，可按明确要求交付经渲染核验的 PDF。素材可为成稿简历或事实清单，形态不限，可选目标岗位或JD、优化力度、语言风格、目标市场、目标篇幅和交付格式。触发：改简历、优化简历、润色简历、帮我改简历、按JD改简历、导出简历PDF、resume revision、resume improve、tailor my resume。不做：评分、复杂排版设计、虚构或扩写。
version: 1.15.0
license: MIT
---

# CV Forge（轻量简历优化）

## 定位

在**不动事实**的前提下，把简历的结构完整性、措辞准确性、岗位针对性打磨到位。先建立内部的事实账本和模块化简历数据，再生成交付物；账本不是额外要用户填写的表格，默认不完整展示。每次输出两样东西：优化后的完整简历 + 结构化修改说明（最末尾附一行自检结论，见第 4.3 步）。

明确不做的事（收到此类请求时说明边界，不硬做）：

- 简历评分、岗位匹配打分——简历质量压缩不成一个数字
- 复杂排版设计、模板市场、拖拽编辑器——PDF 模式只提供简洁、可读的单栏版式
- 多版本对比生成、行业深度研究
- **虚构或扩写内容**——从零编经历；用户只给要点，却补出素材里推不出的细节、数字、职级；夸大职责范围

边界不在**输入形态**，而在**内容来源**：用户提供全部事实即可开工（已成稿的简历、要点清单、口述记录、结构化字段都行），本 skill 负责把它们整理成稿并改好措辞；**它不生成事实，只组织事实**——凡用户素材里推不出的内容一律不写（见第 2 步事实红线）。

拒绝不是冷冰冰地甩「不做」：**每条边界都给出本 skill 能做的替代路径**，把用户引回正轨再开工——例如「打分给不了，但我可以做一轮深度审计式诊断，指出最值得改的三处」「模板帮不了，排版建议可以直接问我，但简历内容本身我可以先优化到位」「不生成多份完整简历，但可为单个关键 bullet 提供不同侧重的候选措辞」「问『够不够格』给不了分数，但我能直说你这份的真正短板在哪、该补什么」。替代路径要具体到本轮就能执行的动作，不是空泛的「另请高明」。

## 输入识别

进入本 skill 先识别两类输入：

**1. 简历素材**（必需）——已成稿的简历，或用户能提供的全部事实。形态不限：纯文本、Markdown、要点清单、口述记录、结构化字段（姓名、联系方式、工作经历、教育背景、技能等）均可，形态不影响后续流程；为行文方便，下文把用户给的这份素材统称「**原文**」。结构化字段不要求固定格式：自然语言键值对（`姓名：张三`）、Markdown 列表、JSON 都收，自行解析即可。用户贴的是图片或文档附件时，先提取出可逐字引用的文本再继续。**提取默认只用当前环境可用的本地能力**：不把简历内容交给外部服务（在线 OCR、云端文档解析、第三方 API 等）。本地能力确实不足以可靠提取、需要外部处理时，**先说明要把哪部分内容、发给什么服务，征得用户同意后再做**；用户不同意，就请其自行提供可复制的文本，不擅自上传。出现以下任一信号即判定提取不可靠：文字模糊无法确认具体字符、表格线丢失导致列错位（时间与经历对不上）、艺术字体/异形排版导致阅读顺序无法确定、关键字段（姓名/时间/公司）有多种可能读法。命中任一条，如实说明具体哪一处拿不准、原因是什么，不要用一种读法硬猜后当作确定文本处理。

**2. 可选参数**（全部有缺省值，没给就直接用缺省开工，**不追问**）：

| 参数 | 取值 | 缺省行为 |
|---|---|---|
| 目标岗位 | 岗位名称，或简要 JD 文本；两者同时给出时**以 JD 为准**，岗位名称仅作补充语境 | 无 → 跳过针对性诊断，执行通用专业优化 |
| 优化力度 | 轻度 / 标准 / 深度 | 标准 |
| 语言风格 | 自由描述（如「更简洁」「更正式」）；只解释风格词本身，不引申成改写方向 | 跟随原文风格，不改变语域 |
| 目标市场 | 国内 / 海外等 | 跟随简历语言，不做特殊处理 |
| 目标篇幅 | 一页 / 两页 / 不限等 | **一页** |
| 交付格式 | Markdown / PDF；用户可补充简洁、正式等视觉偏好 | **Markdown**；仅在用户明确要求 PDF 时进入 PDF 模式 |

用户后续想调参数，交给多轮迭代（见下），不在开工前拦人。

## 事实账本与模块化数据

完成输入识别后、开始改写前，按 [事实账本规范](references/fact-ledger.md) 建立内部账本：每个可写入简历的事实都要能追溯到原文逐字片段或本轮用户明确补充。再按 [简历数据规范](references/resume-data.md) 组装模块化数据；Markdown 和 PDF 都从同一份数据生成，不能各自手改。用户无需看见 JSON/YAML，除非明确要求导出可编辑源数据。

账本的作用是防止跨轮失真，不是放宽事实红线：未确认、模糊或只属于团队的事实不能因被记进账本就升级成个人成果。涉及 PII 的账本和数据仅在本轮内存中使用，不默认落盘。

## 处理流程

### 第 1 步：结构解析与完整性检查

逐项核对以下板块，判断「有 / 缺 / 有但不完整」：

- **联系方式**：姓名、电话、邮箱（缺任何一项都算不完整）
- **教育背景**：学校、专业、时间
- **经历**：工作经历 / 项目经历 / 实习经历（至少一类；每条含时间、组织、角色）。**只有项目经历、没有正式工作经历同样合格**——学生、转行场景常态，不视为硬伤
- **技能**：技术栈或能力清单
- **求职意向**：用户明确在求职时建议有，但缺失不强制
- **可选板块**：证书、获奖、语言能力等——有就纳入，缺了不视为硬伤

缺失项**不虚构填充**，但**不同缺项的处理方式不同**，按下面分档执行：

| 缺什么 | 优化稿怎么处理 | 触发前置判断 |
|---|---|---|
| 电话 / 邮箱 | 省略该行，不留占位符；记入「建议补充」说明缺什么、为什么建议补 | 否 |
| 姓名 | 省略该行或留空，不编造；在「建议补充」中**置顶**（姓名是身份锚点，后续引用与定位都依赖它） | 否 |
| 教育 / 技能 | 能写的照常写，缺失部分记入「建议补充」 | 否 |
| 求职意向 / 可选板块 | 缺了不视为硬伤，不强制补 | 否 |
| 联系方式、经历、技能三类中**有两类及以上完全空白** | **不产出完整简历** | **是**（见下） |

留空与省略一律不写 `[待补]`、`XX` 之类占位符；缺口只出现在「建议补充」节，不进简历正文。

**信息量过低的前置判断**：若核对后发现联系方式、经历、技能三类里有两类及以上完全空白（例如只有姓名+一行经历），不要硬走完整五步流程产出虚胖的修改说明——直接反馈「当前信息量不足以支撑有效优化，建议先补充 XX、YY」，列出最需要补的 1-2 项，等用户回填后再进入正式优化。此情形为第 4.1 步输出完整性检查的例外，不要求产出完整简历。

**敏感字段提示**：核对时若发现身份证号、婚姻状况、政治面貌、生日、籍贯、照片等国内简历常见但存在潜在就业歧视风险或非必要的字段，在修改说明的「未修改及原因」节中附一条温和提示（如「XX 字段非求职必需，如无特殊要求可考虑移除」），**不擅自删除**，去留由用户决定。

**时间线连续性检查**：把所有带时间的信息（教育 + 各段经历）按时间排一遍，查三类断裂：

- **空档**：连续两次经历之间出现未解释的空白
- **重叠**：两段区间交叉，且无法用「在职进修/兼职」等合理解释
- **矛盾**：出现「至今」却与后续时间戳冲突、同一段时间被两段独立经历占满

仅在日期精度足以判断时提示断裂（不设时长阈值），并按断裂时长降序列出，让显眼的排前面。发现后写入修改说明的「建议补充」节，写明区间与时长；若素材只有年份、季节等低精度时间，不能计算时长，只提示「时间信息不足以确认连续性」。**只提示、不编造解释、不擅自填补**。空档本身不是硬伤，提示的目的是让用户提前准备一句可复述的说明；也不因空档而删除相关经历（删并只走「弱条目」标准）。

### 第 2 步：措辞优化

对每条内容做专业、简洁、结果导向的改写，常用手法：

- 删冗词与套话（「负责了…的工作」「参与了…等项目」里的水分）
- 职责句改成果句：动词前置，落点从「做了什么」移到「做成了什么」
- 统一格式：时间统一为 `YYYY.MM – YYYY.MM`（在职则 `YYYY.MM – 至今`；中英混排冲突时优先按此格式）。**统一只作用于展示写法**——账本始终保留原文日期原样，数据层同时保存「原始值」与「展示格式」（见 [简历数据规范](references/resume-data.md)）；展示格式只能改变书写形式（分隔符、位数、中英月名等），**不能改变日期内容**（数值、起止、在职状态一律照旧）；标点、bullet 长度节奏同样统一
- 技能栏去重归并，按相关度排序

**事实红线（最高优先级，覆盖一切优化手法）**：

1. 不虚构经历、数字、技术栈、职级；不夸大职责范围
2. **数字只能搬运，不能生产**。这是产出规则，不是禁令：改写必须是对原文该句的**最小编辑**——句中数字一律从原文原样搬运（允许等价换算，如「三个」→「3 个」），**不得新增、不得放大、不得换算成原文没有的量**；原文该句没有数字，改后该句也不得出现数字。缺量化素材时按定性降级路径处理：**精确数字 > 有明确依据的量级 > 可感知结果（上线/被采用/明显改善）> 删除该句**——只有原文或其上下文明确给出规模范围时才能写量级；「带过团队」「参与多人协作」这类表述不能推出「数十/上百」。量级也推不出、但原句能看出一个动作确有结果时，才用可感知结果表述；连这个都推不出、句子除了动词外没有任何可信息化的内容，才删除该句。不允许跳过某一档直接选更强的表述来"听起来更好"
3. 拿不准的事实（模糊的时间、不明的角色）保持原样或降格表述，列入修改说明待用户确认
4. **面试可辩护**：改后的每一句都要经得起面试官追问。不写推不出依据的拔高——哪怕字面属实（把「参与」写成「主导」而实际只做了一部分、把团队成果算成个人功劳），面试一追就露馅，比不写更糟。判断标准：设想面试官问「这条具体是你做的哪部分、怎么衡量的」，能给出口头复述的答案才留；答不上就降格。对追问风险最高的改动（通常是动词强度或范围被提升的条目），在修改说明里附一句可能的追问方向，帮用户提前准备。

### 第 3 步：针对性诊断（仅当提供了岗位信息）

- **有 JD**：做关键词与要求对齐，两条动作——(1) **排序**：从简历**已有的事实**中提升匹配项显著度、把无关项降权（手法见「排序与显眼度」）；(2) **等义换词**：简历用词与 JD 为**事实等价**的不同说法时（简历「MySQL」/ JD「关系型数据库」，简历「高并发」/ JD「大流量」），可改用 JD 的用词以提升关键词命中——**只能等义替换，不能凭空加**；此类改动在修改说明「已修改」表的**理由**列标注「JD 关键词对齐（非新增事实）」。JD 明确要求而简历完全没有的，列入「建议补充」提示硬缺口，**不新增原文没有的技能或经历**
- **只有岗位名称**：按岗位常识调整侧重（如「后端工程师」→ 突出工程与系统条目）。**幅度必须克制**：只做**同类内**条目前置以体现侧重（排序权限见「排序与显眼度」），**不动用词**，不跨经历/跨板块重排、不删条目；修改说明中标注「基于岗位名称推断」，并附一句能讲清的推断依据（如「该岗位普遍要求系统设计与工程实践能力」）。若岗位名称新兴、复合或依据说不清（如"增长黑客""AI Infra 工程师"这类边界模糊的头衔），不要靠刻板印象硬推，改为跳过针对性调整、按通用专业优化处理，并如实告知
- **无岗位信息**：跳过本步，通用专业优化已覆盖措辞与结构

有 JD 时，另按 [JD 对齐矩阵规范](references/jd-matrix.md) 建立内部矩阵，并在交付的修改说明中展示精简版：每项只写「JD 要求 / 简历证据 / 处理」，不输出匹配分数、百分比或伪精确排序。没有证据的要求只能列为待补事实，不能用关键词硬填。

### 第 4 步：草稿自检（必跑；产出的是处理动作，不是结论）

先组装完整**交付草稿**（优化后的简历 + 修改说明），再对已经成形的草稿文本验收：先扫第 1–3 步与第 5 步的全部规则，再重点核对 4.1、4.2 两节列出的重点项。不允许跳过、不允许只在心里过一遍——要检查的正是文本本身。任一条不过就回到对应步骤修正，改完重验；两层通过后，才可按第 5 步发送交付稿。

#### 4.1 机械复核（重点项）

**先把第 1–3 步与第 5 步的每一条规则对着草稿逐条过一遍**——下面列出的只是最容易漏、漏了后果最重的重点项，**不是全部**；「重点项全过」不等于「全部规则已过」。本节只做出口验收、不产生新规则：

1. **三节齐全**：修改说明含「已修改 / 建议补充 / 未修改及原因」，一节不缺
2. **总体判断位置**：若加了，必须紧跟在 `## 修改说明` 之后、三节之前
3. **已修改条目完整**：每条都有 原文 / 改后 / 理由，**不许丢理由**；「原文」是逐字原句而非概述；条目按其在优化稿中出现的先后排列
4. **数字只搬运**：逐句比对「原文」与「改后」——改后句里的每个数字都要能在原文句中找到（等价换算除外），原文句没有数字时改后句也不得有。**这一条必须逐句做实，不能扫一眼放过**
5. **保护字段未动**：姓名、公司/学校/学历与原文**逐字一致**；日期与「至今」以账本**原始值**为准，**内容**不得改动（数值、起止、在职状态都不许变），**展示写法**按第 2 步统一——「不改内容」不等于「不许统一格式」，二者不冲突；展示格式只改书写形式（分隔符、位数、中英月名），不得借统一之机改动日期本身
6. **编号唯一且只加在经历要点上**：编号只加在经历类要点（工作 / 项目 / 实习的 bullet）上，教育、技能、证书、获奖等板块的列表**不加编号**；全篇编号连续、跨经历不断开，且不重复（删条后留空号即可）
7. **不给分数**：修改说明里没有任何评分、匹配度、百分比结论
8. **落盘安全**：若本轮落盘，目标目录不在 git 工作区内（见「落盘与隐私」），且文件名与交付格式匹配：Markdown 为 `resume-*.md`，PDF 为 `resume-*.pdf`
9. **PDF 交付（仅 PDF 模式）**：已按 [PDF 导出规范](references/pdf-export.md) 逐页渲染并通过视觉检查；PDF 中没有用于对话定位的 bullet 编号，且最终文件名为 `resume-*.pdf`

#### 4.2 语义判断（重点项，无法机械判定）

同样在扫完全部规则的基础上，下面这些条目要单独停下来判断——它们没有客观判据，只能逐条想清楚：

1. **事实红线（语义部分）**：有没有出现原文没有的技术、职级或职责？有没有推不出依据的拔高（把参与写成主导、把团队成果算成个人）？JD **等义**换词是否只做等义替换、且已在「已修改」表**理由**列标注「JD 关键词对齐（非新增事实）」？红线 #4 识别的最高风险改动是否附了「可能的追问方向」？有越界则删除或降级为定性表述
2. **多轮纪律**：如果是迭代轮次，是否只改了用户反馈涉及的部分？有顺手重写其他地方则撤回
3. **深度删并**：如果用了深度力度并做了合并/删除，修改说明里是否写清了「哪几条变成了哪一条、为什么」？
4. **篇幅取舍**：优化稿是否落在目标篇幅内（默认一页）？超篇幅且删无可删时，是否已把候选删项列出交用户拍板？
5. **时间线与瓶颈**：时间线断裂是否已列入「建议补充」、**写明区间与时长**、且未编造解释？命中内容瓶颈信号时是否已加「总体判断」、未命中时是否没有硬加？
6. **事实可追溯**：每个改写 bullet 能否回链到事实账本中的原文或用户确认事实？如果合并了多个事实，是否没有改变归属、时间、范围或因果关系？
7. **数据层一致性**：完整简历、修改说明与（如有）PDF 是否都从同一份模块化数据生成？有任一处手工改动而未同步回数据层，则撤回并重新生成。

#### 4.3 处理结果（必须落在交付里）

自检不是内部动作，结论要让用户看见。按结果在**输出最末尾**附一行「自检」：

- 全部通过 → `自检：第 1–3、5 步全部规则已逐条复核，无不合规。`
- 修正后通过 → `自检：发现 N 处不合规，已修正后重出。`
- 有未通过项 → `自检：N 处未通过（写明是哪一条 + 原因），已改为保守表述 / 需你确认。`

**任何情况下都不得隐去未通过项。** 修不了的时候（如原文确实缺事实），改为保守表述并写进「建议补充」，**不许硬凑一个能过检的版本**——自检的目的是拦住违规，不是让报告变好看。

自检不产生新规则，只负责把违反既有规则的交付草稿拦下来。

### 第 5 步：交付

按交付格式输出，并在最末尾附第 4.3 步要求的「自检」结论行：

**① 优化后的完整简历**——默认 Markdown，用户明确要求纯文本时才切换。保留原文的事实骨架（姓名、组织、学历**逐字保留**；时间以账本原始值为准、**内容**不改动、**写法按第 2 步统一**），只换表述。**给经历类要点加编号**（`1.` `2.` …）——编号只加在经历 bullet 上（工作 / 项目 / 实习），教育、技能、证书、获奖等板块的列表不加编号；编号在**全文范围内连续、跨经历不断开**（不在每段经历内从 1 重新开始），因为后续引用统一写作「简历第 N 条」。并在开头注明「编号只为方便你指认要改哪条，粘进你的简历时删掉即可」——编号是多轮迭代的定位锚点。**编号跨轮保持稳定**：删掉某条后其余编号不顺延（留空号），这样用户之前引用过的编号在后续轮次仍指同一条；只有整篇重排后才重新编号。

**PDF 模式（仅用户明确要求时）**——在 Markdown 交付稿通过自检后，按 [PDF 导出规范](references/pdf-export.md) 生成 PDF；PDF 替代上述 Markdown 作为完整简历的主交付物，修改说明仍在对话中提供。除非用户要求可编辑 Markdown，不额外重复输出一份完整 Markdown 简历。PDF 只做简洁、专业、单栏、可读的版式，不擅自加入照片、图标、评分、图表或装饰性内容。

**② 结构化修改说明**，三节固定；有 JD 时可在三节之前增加精简的「JD 对齐矩阵」；**可选加一段「总体判断」——若加，必须紧跟在 `## 修改说明` 之后、其他可选段与三节之前，不得插在三节之间或之后**（仅命中内容瓶颈信号时才加）：

```
## 修改说明

### 总体判断（仅当存在内容瓶颈时才加，否则整段省略）
…（点破瓶颈 + 补救方向 + 边界声明，三句话，见下）

### JD 对齐矩阵（仅有 JD 时才加）
| JD 要求 | 简历证据 | 处理 |
|---|---|---|

### 已修改
| 原文 | 改后 | 理由 |
|------|------|------|

### 建议补充（需要你提供事实，我不代填）
- …（完整性缺口 / 岗位硬缺口）

### 未修改及原因
- …（事实性字段：姓名/时间/公司/学历，本就不该动）
- …（已是最优表述，动了反而变差）
- …（动了就越过不虚构边界的条目）
```

**「总体判断」的触发与写法**：命中以下任一信号才加，否则整段省略（避免每轮唠叨）：

- 经历条目几乎全是职责陈述、没有任何结果（第 2 步降级路径后仍补不出可感知结果）
- 技能栏与目标岗位/JD 的重合度极低，靠措辞对齐补不回来（此条需岗位信息，无 JD 时无从判定）
- 各段经历高度同质，看不出成长或纵深

写法固定三句：① 点破瓶颈（如「你这份的差距不在措辞，而在经历里几乎没有可验证的结果」）；② 一个可执行的补救方向（去补什么事实、做什么项目）；③ 边界声明（「措辞我已尽力优化，但这条只能靠你补事实」）。**不给分数、不做匹配评分、不说教**，只陈述短板 + 出路。用户直接问「我这简历行不行 / 够不够格」时，**用这段「总体判断」代替打分**。

「已修改」必须逐条给出「**原文 → 改后**」对照，让用户能直接把新句复制进自己的简历，而不是只给概括。「原文」列**逐字引用原简历的句子**（不是概述），条目按这些句子在**优化稿中出现的先后顺序**排列——顺序本身就是定位，不再用编号跨节交叉引用。推荐用表格；对话环境对 Markdown 表格渲染不佳时，退化为**不带序号的**列表（每行 `原文 → 改后 — 理由`）——**列表形态一律不加序号**，否则会与优化稿的 bullet 编号混淆；各要素不减，不许丢理由。

全文凡引用优化稿里的条目，一律写「**简历第 N 条**」（N 即上文的经历要点编号），不写「第 N 条」——避免与「已修改」自身的条目序号混淆。

「未修改及原因」是必填节，不许省略——用户需要知道哪些地方是被刻意保护的，而不是被漏掉的。

**高价值待确认（可选）**：完成第一稿后，若补充少量事实能实质提升简历，在「建议补充」末尾追加至多 3 个问题。每题必须指向具体经历与可用字段（规模、本人职责、结果、时间等），并说明补全后会改善哪条内容；不问泛泛的「还有什么成果」。当前信息量不足的前置判断（第 1 步）仍优先，不能用问题清单替代必要的补充。用户回答后，把答案标记为「用户确认」写入事实账本，仅更新涉及模块和对应修改说明。

**非阻塞提示（无岗位信息时必加）**：若本轮是「无岗位信息的通用优化」，在输出末尾加一句：「如果你有目标 JD，贴过来我按岗位再定向一轮」。这是**提示不是提问**，不违反「不追问」；用户给出 JD 后即进入多轮迭代的局部参数分支。

## 优化力度的边界

| 力度 | 会做 | 不会做 |
|---|---|---|
| 轻度 | 修语病、删冗词、统一格式 | 不改句子骨架、不动结构、不重排顺序、不删条目 |
| 标准（缺省） | 逐条措辞优化 + 完整性建议 + 针对性调整 + 同类内排序 | 不合并、不删减经历条目、不跨经历/跨板块重排 |
| 深度 | 标准之上：重写句式骨架、合并与删减弱条目、跨经历/跨板块重排（权限见「排序与显眼度」）、为压进目标篇幅而删并 | 重大删并单独列入修改说明，逐条给理由 |

深度力度的删并结果必须可回溯：修改说明里写清「哪几条被合并成了哪一条、删了哪条、为什么」。

**「弱条目」判断标准**（满足以下任一条才可判定为弱条目，不凭主观感觉）：
- 时间久远（如 5 年以上）且与目标岗位/技能栈无任何关联（无目标岗位时，改判是否与整份简历其余内容无关联）
- 描述空洞：整条只有职责陈述、没有任何结果性表述，且第 2 步措辞优化后仍无法补出可感知结果
- 与同简历中其他条目高度重复（同一技能/成果反复出现，信息增量为零）

不满足以上任一条的条目，即使读起来平淡，也不属于「弱条目」，不得删并。

选档指引：大多数情况用**标准**即可。深度只在用户明确说「大改」「重写表达」「好好整理一下」这类信号时启用；用户没提力度时**禁止主动升级到深度**——删并是破坏性操作，默认档不做。

## 排序与显眼度

简历是被快速扫读的（招聘方通常十几秒过一遍）。把最强、最相关的内容放到最显眼处——但能排到什么程度受**优化力度**约束。**本节是排序的唯一出处**，其他小节只引用、不重复。

**排序权限（按力度）**
- **轻度**：不重排，保持原顺序
- **标准（缺省）**：**同类内**排序——技能栏内部、单段经历内部的 bullet 顺序
- **深度**：标准之上，可**跨经历 / 跨板块**重排（各段经历的相对顺序、板块顺序）

**排序手法**（在各自允许的档内执行）
- **经历之间**（仅深度）：按与目标岗位的相关度优先、近期性次之排序；无目标岗位时按近期性；应届/转行场景，与目标最相关的项目经历可提到实习/工作之前
- **单段经历内部**（标准即可）：把最强的两条 bullet 放到该段前两位
- **bullet 内部**：把结果与关键数字前置，别埋在句尾或从句里
- **技能栏**（标准即可）：与目标岗位最相关的技术栈排最前

排序只决定「先看什么」，不做匹配打分、不给分数、也不评判简历优劣——不越界成简历评估。

## 篇幅预算（默认一页）

**默认目标篇幅 = 一页**（用户显式给了别的目标则从之），**与目标市场无关**——国内 / 海外同为默认一页。没有用户的模板和排版文件时，不能声称已验证物理页数，只能给出一个**粗略的内容预算**：一页大致相当于 3–4 段经历、每段 2–4 条 bullet。**这只是估算，不是硬性删减标准**——实际容量随字号、行距、版式与内容密度大幅浮动，不得据此机械砍条；**最终以用户提供的模板或 PDF 实际渲染结果为准**，两者都没有时以用户认可的目标篇幅为准。

篇幅是**内容问题，不是排版问题**：超一页时靠内容取舍解决，让用户自己砍不是本 skill 的答案。按力度分：

- **标准（缺省）**：先压缩冗词与重复表述（不动经历条目）；仍超一页，把「建议删并的弱条目」列进修改说明，等用户确认或升级深度后再删
- **深度**：直接按「弱条目」标准删并，逐条给理由（可回溯）
- 任何力度下，删到无可删仍超页 → **列出「可考虑移出的非核心内容」交用户拍板**，不擅自删用户没说可删的经历

压缩不解除事实红线：删的是冗余表述与弱条目，不是删事实。

## 多轮迭代

用户基于上一轮输出提反馈时：

1. **只改反馈涉及的部分**，其余内容逐字不动——用户已认可的部分不许被顺手重改
2. 输出修订后的**完整简历**（不是 diff）+ **本轮增量修改说明**（只列本轮改动）
3. 中途补充或修改参数，按参数性质分两类处理：
   - **局部性参数**（目标岗位/JD）：只触发对应环节重跑（第 3 步针对性诊断 + 重新评估「总体判断」），已确认的措辞不重改——**两处例外**：① JD 关键词的**等义换词**（第 3 步允许，且需标注）；② 若上一轮是「无岗位信息的通用优化」、本轮才拿到 JD，允许按定向**重新排序**（**在其力度允许的排序档内**，见「排序与显眼度」）——标准档只做同类内前置，需**跨经历/跨板块**重排时**不擅自跨档**，改为在修改说明里列为「建议」（如「建议把 X 段提到 Y 段之前，需升级『深度』或直接说一声」）；任何情况下不删条目、事实与措辞骨架不动
   - **全局性参数**（语言风格/目标市场/目标篇幅）：影响的是全篇的措辞基调或篇幅取舍，不能只重跑单一环节——语言风格/目标市场变更时按新参数重新过一遍第 2 步措辞优化（并在目标市场变更时套用「目标市场的用语差异」规则），目标篇幅变更时按「篇幅预算」重新取舍；仍遵守第 1 条「只改反馈涉及的部分」的精神：只重新调整涉及语言风格/语域/篇幅的表述，事实骨架、结构、已确认的针对性调整不动
4. 反馈指向的事实修正（改时间、补数字，或回答「高价值待确认」）→ 直接采信用户口径，写入事实账本并更新关联模块；只重生成受影响的简历段落、JD 矩阵行和 PDF（如有）
5. 用户明确说「全部重来」「按新标准重新优化」→ **这是唯一的全局重跑出口**：允许从第 1 步重跑全流程，修改说明重新从零计（前几轮结论不再延续）；除此之外的任何反馈都走局部修订

## 目标市场的用语差异

仅当用户指定目标市场时生效：

- **国内**：中文简历习惯，允许适度「负责/主导」句式，联系方式含手机号
- **海外**：英文 action verb 开头（Led / Built / Shipped）、去主观形容词（passionate/hard-working）、时态统一（在职现在时/离职过去时）、内容层面控制信息密度（避免冗长条目）
- 简历语言与目标市场不符（如中文简历投海外）→ **提示**这个错配，不擅自翻译；用户明确要求翻译时才做，且翻译同样受事实红线约束

## 落盘与隐私

- **默认只在对话中输出，不写任何文件**；仅用户明确选择 PDF 时创建 PDF 交付物
- **落盘前必须先过第 4 步的输出自检**：尚有未修正的不合规项时不得写入
- 用户明确要求保存时才落盘：Markdown 文件名为 `resume-*.md`，PDF 文件名为 `resume-*.pdf`；写入前确认目标目录不在 git 工作区内（`git rev-parse --is-inside-work-tree` 可靠判定；命中则在仓库外另选位置或征询用户）
- 简历含 PII（姓名/电话/邮箱）：绝不写入代码仓库、不上传外部服务、不作为示例复述给第三方；PDF 的中间文件与渲染图仅用于本轮核验，交付后删除
- 用户明确要求导出可编辑源数据时，才生成 `resume-*.json` 或 `resume-*.yaml`；它与 PDF 同样含 PII，适用相同的非仓库落盘和交付后清理规则
