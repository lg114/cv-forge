---
name: cv-forge
description: >
  轻量简历优化。在不虚构、不夸大的前提下，改进结构完整性、措辞与岗位针对性，
  输出完整优化简历 + 结构化修改说明。支持纯文本/结构化输入，可选目标岗位或JD、
  优化力度、语言风格、目标市场、目标篇幅，支持多轮迭代。
  触发：改简历、优化简历、润色简历、帮我改简历、按JD改简历、
  resume revision、resume improve、tailor my resume。
  不做：简历评分、排版设计、从零代写、虚构经历。
version: 1.5.0
license: MIT
agent_created: true
---

# CV Forge（轻量简历优化）

## 定位

在**不动事实**的前提下，把简历的结构完整性、措辞准确性、岗位针对性打磨到位。每次输出两样东西：优化后的完整简历 + 结构化修改说明。

明确不做的事（收到此类请求时说明边界，不硬做）：

- 简历评分、岗位匹配打分——简历质量压缩不成一个数字
- 排版模板、视觉美化——只管内容，不管长什么样
- 多版本对比生成、行业深度研究
- 从零代写、虚构经历、夸大职责

拒绝不是冷冰冰地甩「不做」：**每条边界都给出本 skill 能做的替代路径**，把用户引回正轨再开工——例如「打分给不了，但我可以做一轮深度审计式诊断，指出最值得改的三处」「模板帮不了，排版建议可以直接问我，但简历内容本身我可以先优化到位」「多版本对比生成不了，但同一版本里某个关键 bullet 可以按不同侧重各写一版供你挑」「问『够不够格』给不了分数，但我能直说你这份的真正短板在哪、该补什么」。替代路径要具体到本轮就能执行的动作，不是空泛的「另请高明」。

## 输入识别

进入本 skill 先识别两类输入：

**1. 简历本体**（必需）。纯文本、Markdown 或结构化字段（姓名、联系方式、工作经历、教育背景、技能等）均可，形态不影响后续流程。结构化字段不要求固定格式：自然语言键值对（`姓名：张三`）、Markdown 列表、JSON 都收，自行解析即可。用户贴的是图片或文档附件时，先提取出可逐字引用的文本再继续；出现以下任一信号即判定提取不可靠：文字模糊无法确认具体字符、表格线丢失导致列错位（时间与经历对不上）、艺术字体/异形排版导致阅读顺序无法确定、关键字段（姓名/时间/公司）有多种可能读法。命中任一条，如实说明具体哪一处拿不准、原因是什么，不要用一种读法硬猜后当作确定文本处理。

**2. 可选参数**（全部有缺省值，没给就直接用缺省开工，**不追问**）：

| 参数 | 取值 | 缺省行为 |
|---|---|---|
| 目标岗位 | 岗位名称，或简要 JD 文本；两者同时给出时**以 JD 为准**，岗位名称仅作补充语境 | 无 → 跳过针对性诊断，执行通用专业优化 |
| 优化力度 | 轻度 / 标准 / 深度 | 标准 |
| 语言风格 | 自由描述（如「更简洁」「更正式」）；只解释风格词本身，不引申成改写方向 | 跟随原文风格，不改变语域 |
| 目标市场 | 国内 / 海外等 | 跟随简历语言，不做特殊处理 |
| 目标篇幅 | 一页 / 两页 / 不限等 | **一页** |

用户后续想调参数，交给多轮迭代（见下），不在开工前拦人。

## 处理流程

### 第 1 步：结构解析与完整性检查

逐项核对以下板块，判断「有 / 缺 / 有但不完整」：

- **联系方式**：姓名、电话、邮箱（缺任何一项都算不完整）
- **教育背景**：学校、专业、时间
- **经历**：工作经历 / 项目经历 / 实习经历（至少一类；每条含时间、组织、角色）。**只有项目经历、没有正式工作经历同样合格**——学生、转行场景常态，不视为硬伤
- **技能**：技术栈或能力清单
- **求职意向**：用户明确在求职时建议有，但缺失不强制
- **可选板块**：证书、获奖、语言能力等——有就纳入，缺了不视为硬伤

缺失项**不虚构填充**：结构性缺口记入修改说明的「建议补充」节，说明缺什么、为什么建议补，等用户在下一轮提供事实。

**信息量过低的前置判断**：若核对后发现联系方式、经历、技能三类里有两类及以上完全空白（例如只有姓名+一行经历），不要硬走完整五步流程产出虚胖的修改说明——直接反馈「当前信息量不足以支撑有效优化，建议先补充 XX、YY」，列出最需要补的 1-2 项，等用户回填后再进入正式优化。此情形为第 4 步输出完整性检查的例外，不要求产出完整简历。

**敏感字段提示**：核对时若发现身份证号、婚姻状况、政治面貌、生日、籍贯、照片等国内简历常见但存在潜在就业歧视风险或非必要的字段，在修改说明的「未修改及原因」节中附一条温和提示（如「XX 字段非求职必需，如无特殊要求可考虑移除」），**不擅自删除**，去留由用户决定。

**时间线连续性检查**：把所有带时间的信息（教育 + 各段经历）按时间排一遍，查三类断裂：

- **空档**：连续两次经历之间出现未解释的空白
- **重叠**：两段区间交叉，且无法用「在职进修/兼职」等合理解释
- **矛盾**：出现「至今」却与后续时间戳冲突、同一段时间被两段独立经历占满

**任何断开都提示**（不设时长阈值），但按断裂时长降序列出，让显眼的排前面。发现后写入修改说明的「建议补充」节（写明区间与时长），**只提示、不编造解释、不擅自填补**。空档本身不是硬伤，提示的目的是让用户提前准备一句可复述的说明；也不因空档而删除相关经历（删并只走「弱条目」标准）。

### 第 2 步：措辞优化

对每条内容做专业、简洁、结果导向的改写，常用手法：

- 删冗词与套话（「负责了…的工作」「参与了…等项目」里的水分）
- 职责句改成果句：动词前置，落点从「做了什么」移到「做成了什么」
- 统一格式：时间统一为 `YYYY.MM – YYYY.MM`（在职则 `YYYY.MM – 至今`；中英混排冲突时优先按此格式）、标点、bullet 长度节奏
- 技能栏去重归并，按相关度排序

**事实红线（最高优先级，覆盖一切优化手法）**：

1. 不虚构经历、数字、技术栈、职级；不夸大职责范围
2. 原文没有的数字禁止出现在改写里。缺量化素材时按定性降级路径处理：**精确数字 > 量级（数十/上百）> 可感知结果（上线/被采用/明显改善）> 删除该句**——判断该落哪一档看能否从原文（含上下文，如时间跨度、团队规模、职级）直接或间接推出：能推出具体数字用数字；只能推出大致量级（如「带过团队」但无人数，只知道「不是一个人做的」）用量级；量级也推不出、但原句能看出一个动作确有结果（上线了/被采用了/被认可了）用可感知结果表述；连这个都推不出、句子除了动词外没有任何可信息化的内容，才删除该句。不允许跳过某一档直接选更强的表述来"听起来更好"
3. 拿不准的事实（模糊的时间、不明的角色）保持原样或降格表述，列入修改说明待用户确认
4. **面试可辩护**：改后的每一句都要经得起面试官追问。不写推不出依据的拔高——哪怕字面属实（把「参与」写成「主导」而实际只做了一部分、把团队成果算成个人功劳），面试一追就露馅，比不写更糟。判断标准：设想面试官问「这条具体是你做的哪部分、怎么衡量的」，能给出口头复述的答案才留；答不上就降格。对追问风险最高的改动（通常是动词强度或范围被提升的条目），在修改说明里附一句可能的追问方向，帮用户提前准备。

### 第 3 步：针对性诊断（仅当提供了岗位信息）

- **有 JD**：做关键词与要求对齐，两条动作——(1) **排序**：从简历**已有的事实**中提升匹配项显著度、把无关项降权（手法见「排序与显眼度」）；(2) **等义换词**：简历用词与 JD 为**事实等价**的不同说法时（简历「MySQL」/ JD「关系型数据库」，简历「高并发」/ JD「大流量」），可改用 JD 的用词以提升关键词命中——**只能等义替换，不能凭空加**；此类改动在修改说明里标注「JD 关键词对齐（非新增事实）」。JD 明确要求而简历完全没有的，列入「建议补充」提示硬缺口，**不新增原文没有的技能或经历**
- **只有岗位名称**：按岗位常识调整侧重（如「后端工程师」→ 突出工程与系统条目）。**幅度必须克制**：只做条目前置与措辞侧重，不重组结构、不删条目；修改说明中标注「基于岗位名称推断」，并附一句能讲清的推断依据（如「该岗位普遍要求系统设计与工程实践能力」）。若岗位名称新兴、复合或依据说不清（如"增长黑客""AI Infra 工程师"这类边界模糊的头衔），不要靠刻板印象硬推，改为跳过针对性调整、按通用专业优化处理，并如实告知
- **无岗位信息**：跳过本步，通用专业优化已覆盖措辞与结构

### 第 4 步：Pre-send check

发送前逐项自检，任一不通过则先修正再输出，不允许跳过：

1. **事实红线**：有没有出现原文没有的数字、技术、职级或职责？有没有推不出依据的拔高（把参与写成主导、把团队成果算成个人）？JD 同义换词是否只做等义替换、且已标注「JD 关键词对齐」？有越界则删除或降级为定性表述
2. **保护字段**：姓名、时间、公司/学校、学历是否被改动？有则还原（用户在多轮中明确给出的事实修正除外）
3. **输出完整性**：是否同时包含「优化后的完整简历」+「修改说明」三节（已修改 / 建议补充 / 未修改及原因）？缺任何一节则补全；无岗位信息的轮次是否附了「再定向一轮」提示？
4. **多轮纪律**：如果是迭代轮次，是否只改了用户反馈涉及的部分？有顺手重写其他地方则撤回
5. **深度删并**：如果使用了深度力度并做了合并/删除，修改说明里是否写清了「哪几条变成了哪一条、为什么」？没有则补上
6. **篇幅与编号**：优化稿是否落在目标篇幅内（默认一页）？每条 bullet 的编号是否贯穿全文、且与修改说明「位置」列对得上？超篇幅且删无可删时，是否已把候选删项列出交用户拍板？
7. **时间线与瓶颈**：时间线断裂是否已列入「建议补充」且未编造解释？命中内容瓶颈信号时是否已加「总体判断」、未命中时是否没有硬加？

全部通过后才进入第 5 步输出。这是最后闸门，前面各步的规则在这里做一次出口验收——自检不产生新规则，只负责把违反既有规则的产物拦下来。

### 第 5 步：输出

依次输出两部分：

**① 优化后的完整简历**——默认 Markdown，用户明确要求纯文本时才切换。保留原文的事实骨架（姓名、时间、组织、学历等逐字保留），只换表述。**给每条 bullet 加连续编号**（`1.` `2.` …），并在开头注明「编号只为方便你指认要改哪条，粘进你的简历时删掉即可」——编号是多轮迭代的定位锚点。

**② 结构化修改说明**，三节固定，**开头可选加一段「总体判断」**（仅命中内容瓶颈信号时才加）：

```
## 修改说明

### 总体判断（仅当存在内容瓶颈时才加，否则整段省略）
…（点破瓶颈 + 补救方向 + 边界声明，三句话，见下）

### 已修改
| # | 位置 | 原文 | 改后 | 理由 |
|---|------|------|------|------|

### 建议补充（需要你提供事实，我不代填）
- …（完整性缺口 / 岗位硬缺口）

### 未修改及原因
- …（事实性字段：姓名/时间/公司/学历，本就不该动）
- …（已是最优表述，动了反而变差）
- …（动了就越过不虚构边界的条目）
```

**「总体判断」的触发与写法**：命中以下任一信号才加，否则整段省略（避免每轮唠叨）：

- 经历条目几乎全是职责陈述、没有任何结果（第 2 步降级路径后仍补不出可感知结果）
- 技能栏与目标岗位/JD 的重合度极低，靠措辞对齐补不回来（此条需岗位信息，无 JD 时无从判定）
- 各段经历高度同质，看不出成长或纵深

写法固定三句：① 点破瓶颈（如「你这份的差距不在措辞，而在经历里几乎没有可验证的结果」）；② 一个可执行的补救方向（去补什么事实、做什么项目）；③ 边界声明（「措辞我已尽力优化，但这条只能靠你补事实」）。**不给分数、不做匹配评分、不说教**，只陈述短板 + 出路。用户直接问「我这简历行不行 / 够不够格」时，**用这段「总体判断」代替打分**。

「已修改」必须逐条给出「**原文 → 改后**」对照，让用户能直接把新句复制进自己的简历，而不是只给概括；「位置」列引用优化稿里的 bullet 编号。推荐用表格；对话环境对 Markdown 表格渲染不佳时，退化为编号列表（`[编号] 位置 — 原文 → 改后 — 理由`），各要素不减，不许丢理由。

「未修改及原因」是必填节，不许省略——用户需要知道哪些地方是被刻意保护的，而不是被漏掉的。

**非阻塞提示（可选）**：若本轮是「无岗位信息的通用优化」，在输出末尾加一句：「如果你有目标 JD，贴过来我按岗位再定向一轮」。这是**提示不是提问**，不违反「不追问」；用户给出 JD 后即进入多轮迭代的局部参数分支。

## 优化力度的边界

| 力度 | 会做 | 不会做 |
|---|---|---|
| 轻度 | 修语病、删冗词、统一格式 | 不改句子骨架、不动结构、不删条目 |
| 标准（缺省） | 逐条措辞优化 + 完整性建议 + 针对性调整 | 不合并、不删减经历条目 |
| 深度 | 标准之上：重写句式骨架、合并与删减弱条目、调整板块与条目顺序、为压进目标篇幅而删并 | 重大删并单独列入修改说明，逐条给理由 |

深度力度的删并结果必须可回溯：修改说明里写清「哪几条被合并成了哪一条、删了哪条、为什么」。

**「弱条目」判断标准**（满足以下任一条才可判定为弱条目，不凭主观感觉）：
- 时间久远（如 5 年以上）且与目标岗位/技能栈无任何关联
- 描述空洞：整条只有职责陈述、没有任何结果性表述，且第 2 步措辞优化后仍无法补出可感知结果
- 与同简历中其他条目高度重复（同一技能/成果反复出现，信息增量为零）

不满足以上任一条的条目，即使读起来平淡，也不属于「弱条目」，不得删并。

选档指引：大多数情况用**标准**即可。深度只在用户明确说「大改」「重写表达」「好好整理一下」这类信号时启用；用户没提力度时**禁止主动升级到深度**——删并是破坏性操作，默认档不做。

## 排序与显眼度

简历是被快速扫读的（招聘方通常十几秒过一遍）。默认把最强、最相关的内容放到最显眼处：

- **经历之间**：按与目标岗位的相关度和近期性排序；应届/转行场景，与目标最相关的项目经历可提到实习/工作之前
- **经历内部**：最近一段放最前，其**前两条 bullet 承载最强证据**（硬结果、与目标岗位最相关）
- **bullet 内部**：把结果与关键数字前置，别埋在句尾或从句里
- **技能栏**：与目标岗位最相关的技术栈排最前

排序只决定「先看什么」，不做匹配打分、不给分数、也不判断「够不够格」——不越界成简历评估。

## 篇幅预算（默认一页）

**默认目标篇幅 = 一页**（用户显式给了别的目标则从之）。一页大致容纳 3–4 段经历、每段 2–4 条 bullet——超出就要取舍。

篇幅是**内容问题，不是排版问题**：超一页时靠内容取舍解决，让用户自己砍不是本 skill 的答案。按力度分：

- **标准（缺省）**：先压缩冗词与重复表述（不动经历条目）；仍超一页，把「建议删并的弱条目」列进修改说明，等用户确认或升级深度后再删
- **深度**：直接按「弱条目」标准删并，逐条给理由（可回溯）
- 任何力度下，删到无可删仍超页 → **列出「可考虑移出的非核心内容」交用户拍板**，不擅自删用户没说可删的经历

压缩不解除事实红线：删的是冗余表述与弱条目，不是删事实。

## 多轮迭代

用户基于上一轮输出提反馈时：

1. **只改反馈涉及的部分**，其余内容逐字不动——用户已认可的部分不许被顺手重改
2. 输出修订后的**完整简历**（不是 diff）+ **本轮增量修改说明**（只列本轮改动）
3. 中途补充或修改参数，按参数性质分两类处理：
   - **局部性参数**（目标岗位/JD）：只触发对应环节重跑（第 3 步针对性诊断 + 重新评估「总体判断」），已确认的措辞不重改——**两处例外**：① JD 关键词的**等义换词**（第 3 步允许，且需标注）；② 若上一轮是「无岗位信息的通用优化」、本轮才拿到 JD，允许按定向**重新排序**（条目前置、板块内顺序可调），但不删条目、事实与措辞骨架不动——别让结果半通用半定向
   - **全局性参数**（语言风格/目标市场/目标篇幅）：影响的是全篇的措辞基调或篇幅取舍，不能只重跑单一环节——语言风格/目标市场变更时按新参数重新过一遍第 2 步措辞优化（并在目标市场变更时套用「目标市场的用语差异」规则），目标篇幅变更时按「篇幅预算」重新取舍；仍遵守第 1 条「只改反馈涉及的部分」的精神：只重新调整涉及语言风格/语域/篇幅的表述，事实骨架、结构、已确认的针对性调整不动
4. 反馈指向的事实修正（改时间、补数字）→ 直接采信用户口径，落入下一轮的事实骨架
5. 用户明确说「全部重来」「按新标准重新优化」→ **这是唯一的全局重跑出口**：允许从第 1 步重跑全流程，修改说明重新从零计（前几轮结论不再延续）；除此之外的任何反馈都走局部修订

## 目标市场的用语差异

仅当用户指定目标市场时生效：

- **国内**：中文简历习惯，允许适度「负责/主导」句式，联系方式含手机号
- **海外**：英文 action verb 开头（Led / Built / Shipped）、去主观形容词（passionate/hard-working）、时态统一（在职现在时/离职过去时）、内容层面控制信息密度（避免冗长条目，但不涉及排版页数——排版不在本 skill 范围）
- 简历语言与目标市场不符（如中文简历投海外）→ **提示**这个错配，不擅自翻译；用户明确要求翻译时才做，且翻译同样受事实红线约束

## 落盘与隐私

- **默认只在对话中输出，不写任何文件**
- 用户明确要求保存时才落盘：文件名 `resume-*.md`，写入前确认目标目录不在 git 工作区内（`git rev-parse --is-inside-work-tree` 可靠判定；命中则在仓库外另选位置或征询用户）
- 简历含 PII（姓名/电话/邮箱）：绝不写入代码仓库、不上传外部服务、不作为示例复述给第三方
