---
name: cv-forge
description: 轻量简历优化：在不虚构、不夸大事实的前提下，改进结构完整性、措辞准确性与岗位针对性，输出优化简历及结构化修改说明。支持纯文本或结构化输入，可选目标岗位/JD、优化力度、语言风格、目标市场，支持多轮迭代。触发词：改简历、优化简历、帮我改简历、润色简历、简历润色、简历优化一下、按 JD 改简历、resume revision、resume improve、tailor my resume。不做简历评分、排版设计、虚构代写。
version: 1.1.0
license: MIT
agent_created: true
---

# CV Forge（轻量简历优化）

## 定位

在**不动事实**的前提下，把简历的结构完整性、措辞准确性、岗位针对性打磨到位。每次输出两样东西：优化后的完整简历 + 结构化修改说明。

明确不做的事（收到此类请求时说明边界，不硬做）：

- 简历评分、岗位匹配打分——简历质量压缩不成一个数字
- 排版模板、视觉美化——只管内容，不管长什么样
- 多版本对比生成、行业深度研究
- 从零代写、虚构经历、夸大职责

拒绝不是冷冰冰地甩「不做」：**每条边界都给出本 skill 能做的替代路径**，把用户引回正轨再开工——例如「打分给不了，但我可以做一轮深度审计式诊断，指出最值得改的三处」「模板帮不了，排版建议可以直接问我，但简历内容本身我可以先优化到位」。替代路径要具体到本轮就能执行的动作，不是空泛的「另请高明」。

## 输入识别

进入本 skill 先识别两类输入：

**1. 简历本体**（必需）。纯文本、Markdown 或结构化字段（姓名、联系方式、工作经历、教育背景、技能等）均可，形态不影响后续流程。结构化字段不要求固定格式：自然语言键值对（`姓名：张三`）、Markdown 列表、JSON 都收，自行解析即可。用户贴的是图片或文档附件时，先提取出可逐字引用的文本再继续；提取不可靠（扫描件、排版复杂）就如实说明哪里拿不准，不要硬猜。

**2. 可选参数**（全部有缺省值，没给就直接用缺省开工，**不追问**）：

| 参数 | 取值 | 缺省行为 |
|---|---|---|
| 目标岗位 | 岗位名称，或简要 JD 文本；两者同时给出时**以 JD 为准**，岗位名称仅作补充语境 | 无 → 跳过针对性诊断，执行通用专业优化 |
| 优化力度 | 轻度 / 标准 / 深度 | 标准 |
| 语言风格 | 自由描述（如「更简洁」「更正式」）；只解释风格词本身，不引申成改写方向 | 跟随原文风格，不改变语域 |
| 目标市场 | 国内 / 海外等 | 跟随简历语言，不做特殊处理 |

用户后续想调参数，交给多轮迭代（见下），不在开工前拦人。

## 处理流程

### 第 1 步：结构解析与完整性检查

逐项核对以下板块，判断「有 / 缺 / 有但不完整」：

- **联系方式**：姓名、电话、邮箱（缺任何一项都算不完整）
- **教育背景**：学校、专业、时间
- **经历**：工作经历 / 项目经历 / 实习经历（至少一类；每条含时间、组织、角色）。**只有项目经历、没有正式工作经历同样合格**——学生、转行场景常态，不视为硬伤
- **技能**：技术栈或能力清单
- **求职意向**：用户明确在求职时建议有，但缺失不强制
- **可选板块**：证书、获奖、语言能力等——有就纳入，缺了不视为硬伤

缺失项**不虚构填充**：结构性缺口记入修改说明的「建议补充」节，说明缺什么、为什么建议补，等用户在下一轮提供事实。

### 第 2 步：措辞优化

对每条内容做专业、简洁、结果导向的改写，常用手法：

- 删冗词与套话（「负责了…的工作」「参与了…等项目」里的水分）
- 职责句改成果句：动词前置，落点从「做了什么」移到「做成了什么」
- 统一格式：时间统一为 `YYYY.MM – YYYY.MM`（在职则 `YYYY.MM – 至今`；中英混排冲突时优先按此格式）、标点、bullet 长度节奏
- 技能栏去重归并，按相关度排序

**事实红线（最高优先级，覆盖一切优化手法）**：

1. 不虚构经历、数字、技术栈、职级；不夸大职责范围
2. 原文没有的数字禁止出现在改写里。缺量化素材时按定性降级路径处理：**精确数字 > 量级（数十/上百）> 可感知结果（上线/被采用/明显改善）> 删除该句**——四档里能落到哪档就哪档，都不行就删
3. 拿不准的事实（模糊的时间、不明的角色）保持原样或降格表述，列入修改说明待用户确认

### 第 3 步：针对性诊断（仅当提供了岗位信息）

- **有 JD**：做关键词与要求对齐——从简历**已有的事实**中提升匹配项的显著度（前置、强化措辞），把无关项降权。JD 明确要求而简历完全没有的，列入「建议补充」提示硬缺口，**不新增原文没有的技能或经历**
- **只有岗位名称**：按岗位常识调整侧重（如「后端工程师」→ 突出工程与系统条目）。**幅度必须克制**：只做条目前置与措辞侧重，不重组结构、不删条目；修改说明中标注「基于岗位名称推断」
- **无岗位信息**：跳过本步，通用专业优化已覆盖措辞与结构

### 第 4 步：Pre-send check

发送前逐项自检，任一不通过则先修正再输出，不允许跳过：

1. **事实红线**：有没有出现原文没有的数字、技术、职级或职责？有则删除或降级为定性表述
2. **保护字段**：姓名、时间、公司/学校、学历是否被改动？有则还原（用户在多轮中明确给出的事实修正除外）
3. **输出完整性**：是否同时包含「优化后的完整简历」+「修改说明」三节（已修改 / 建议补充 / 未修改及原因）？缺任何一节则补全
4. **多轮纪律**：如果是迭代轮次，是否只改了用户反馈涉及的部分？有顺手重写其他地方则撤回
5. **深度删并**：如果使用了深度力度并做了合并/删除，修改说明里是否写清了「哪几条变成了哪一条、为什么」？没有则补上

全部通过后才进入第 5 步输出。这是最后闸门，前面各步的规则在这里做一次出口验收——自检不产生新规则，只负责把违反既有规则的产物拦下来。

### 第 5 步：输出

依次输出两部分：

**① 优化后的完整简历**——默认 Markdown，用户明确要求纯文本时才切换。保留原文的事实骨架（姓名、时间、组织、学历等逐字保留），只换表述。

**② 结构化修改说明**，固定三节：

```
## 修改说明

### 已修改
| # | 位置 | 改动摘要 | 理由 |
|---|------|----------|------|

### 建议补充（需要你提供事实，我不代填）
- …（完整性缺口 / 岗位硬缺口）

### 未修改及原因
- …（事实性字段：姓名/时间/公司/学历，本就不该动）
- …（已是最优表述，动了反而变差）
- …（动了就越过不虚构边界的条目）
```

「已修改」推荐用表格；对话环境对 Markdown 表格渲染不佳时，退化为编号列表（`1. 位置 — 改动摘要 — 理由`），四个信息要素不变，不许丢理由。

「未修改及原因」是必填节，不许省略——用户需要知道哪些地方是被刻意保护的，而不是被漏掉的。

## 优化力度的边界

| 力度 | 会做 | 不会做 |
|---|---|---|
| 轻度 | 修语病、删冗词、统一格式 | 不改句子骨架、不动结构、不删条目 |
| 标准（缺省） | 逐条措辞优化 + 完整性建议 + 针对性调整 | 不合并、不删减经历条目 |
| 深度 | 标准之上：重写句式骨架、合并与删减弱条目、调整板块与条目顺序 | 重大删并单独列入修改说明，逐条给理由 |

深度力度的删并结果必须可回溯：修改说明里写清「哪几条被合并成了哪一条、删了哪条、为什么」。

选档指引：大多数情况用**标准**即可。深度只在用户明确说「大改」「重写表达」「好好整理一下」这类信号时启用；用户没提力度时**禁止主动升级到深度**——删并是破坏性操作，默认档不做。

## 多轮迭代

用户基于上一轮输出提反馈时：

1. **只改反馈涉及的部分**，其余内容逐字不动——用户已认可的部分不许被顺手重改
2. 输出修订后的**完整简历**（不是 diff）+ **本轮增量修改说明**（只列本轮改动）
3. 中途补充新参数（如补了 JD）→ 新参数只触发对应环节重跑（针对性诊断），已确认的措辞不重改
4. 反馈指向的事实修正（改时间、补数字）→ 直接采信用户口径，落入下一轮的事实骨架
5. 用户明确说「全部重来」「按新标准重新优化」→ **这是唯一的全局重跑出口**：允许从第 1 步重跑全流程，修改说明重新从零计（前几轮结论不再延续）；除此之外的任何反馈都走局部修订

## 目标市场的用语差异

仅当用户指定目标市场时生效：

- **国内**：中文简历习惯，允许适度「负责/主导」句式，联系方式含手机号
- **海外**：英文 action verb 开头（Led / Built / Shipped）、去主观形容词（passionate/hard-working）、时态统一（在职现在时/离职过去时）、内容层面控制信息密度（避免冗长条目，但不涉及排版页数——排版不在本 skill 范围）
- 简历语言与目标市场不符（如中文简历投海外）→ **提示**这个错配，不擅自翻译；用户明确要求翻译时才做，且翻译同样受事实红线约束

## 落盘与隐私

- **默认只在对话中输出，不写任何文件**
- 用户明确要求保存时才落盘：文件名 `resume-*.md`，写入前确认目标目录不在 git 工作区内（`git rev-parse --is-inside-work-tree` 可靠判定；命中则在仓库外另选位置或征询用户）
- 简历含 PII（姓名/电话/邮箱）：绝不写入代码仓库、不上传外部服务、不作为示例复述给第三方
