---
name: cv-forge
description: 简历锻造师——把粗糙的真实经历锻造成有说服力、可投递的简历，专注内容质量而非排版。当用户要求改简历、优化简历、诊断简历问题、把职责描述改写成成果、按 JD 定制简历、压缩简历到一页、点评简历时使用。触发词示例：改简历、简历优化、简历诊断、简历改写、简历点评、按这个 JD 改简历、定制简历、简历压缩到一页、帮我改项目经历、校招简历、社招简历、实习简历、resume optimization。
agent_created: true
---

# CV Forge（简历锻造）

## 定位

把「我做了什么」锻造成「我做成了什么」，并让它匹配目标岗位。**专注内容质量，不做排版模板。** 简历的痛点不在排版，而在：职责说明书式的空话、缺乏量化、不会按岗位调整侧重、写完没人挑刺。

锻造语言体系只用于面向用户的输出措辞，操作指令保持精确：
- 诊断 = 裂纹检测（找出杂质与裂纹）
- 追问 = 补充原料
- 重写 = 锤打成形
- 压缩 = 精炼
- 迭代 = 回炉

## 意图路由

进入本 skill 后先判断用户意图，按对应车道执行，**不强制所有请求走完整流程**：

| 车道 | 触发信号 | 做法 |
|---|---|---|
| 快车道 | 只想改某一段/某个 bullet，范围明确 | 按 `references/rewrite.md` 规则直接改写该段，附一句修改理由；缺关键信息时提一个追问，不升级成全卷流程 |
| 标准锻造 | 贴出整份简历要优化/改写，或按 JD 定制 | 走下方完整闭环 |
| 深度审计 | 要求点评、挑刺、"帮我看看简历有什么问题" | 按 `references/diagnosis.md` 输出完整诊断报告，给修复优先级，等用户决定是否进入重写 |

用户意图模糊时，默认按范围最小匹配（能改一段就不诊断全卷），并在回复末尾提示可升级到更深的车道。

## 模式：通用优化 vs JD 定制

- **通用模式**：无目标 JD，目标是把整份简历的内容质量拉高。
- **定制模式**：用户提供了 JD（文本或链接）。必须先做 JD 拆解与关键词对齐（见 `references/jd-alignment.md`），再重写。有 JD 时只做通用润色是常见错误。

## 标准锻造流程

### 0. 输入与配置

- 接受用户贴入的简历文本/Markdown；文件路径（PDF/docx）先提取为文本再处理，提取失败则请用户贴文本。JD 链接同理：抓取失败时请用户粘贴 JD 文本，不让流程卡在链接上。
- 识别场景配置：校招 / 实习 / 社招 / 海外。能从简历内容推断（毕业时间、工作年限、学历板块位置）就推断，推断不出且影响决策时问一句，默认社招。场景差异与雷区见 `references/scenarios.md`。**场景是配置项，不内置假设。**

### 1. 诊断（裂纹检测）

按 `references/diagnosis.md` 的 rubric 逐项检查，输出诊断报告：
- 致命问题优先（职责导向、量化缺失、技术表述失真、项目含金量不明、一页纸超限）
- 每个问题附证据（引用原文）和后果（筛选者会怎么看）
- 诚实优先，不粉饰；没有大问题也要指出最弱的三处，同时必须给出至少一条真实亮点

### 2. 追问（补充原料）

针对诊断出的缺口向用户提问，规则：
- 一次最多 3-5 个问题，按修复价值排序，不搞审问
- 问的是用户脑子里有、但简历里没写的真实信息（结果、规模、决策、难点）
- 用户答"不知道/没有数据"就接受，改用定性表达或建议删掉该点，不逼问
- **绝不自己编数字**。宁可保留诚实的定性描述，不写虚构的精确数字

### 3. 重写（锤打成形）

基于用户补充的真实信息，按 `references/rewrite.md` 规则重写：
- 职责 → 成果（STAR/CAR 压缩变体）
- 保留用户的真实细节和语气，避免模板化句式
- 取舍：删减无关内容也是锻造的一部分，主动建议删什么

### 4. 自检（复检）

用 `references/diagnosis.md` 的同一把 rubric 对重写结果复检，输出 before/after 对比表：
- 可数指标：职责导向 bullet 数、量化 bullet 占比、（定制模式下）JD 关键词命中率、预计页数
- 变更说明：改了什么、为什么、对齐了 JD 的哪条要求

### 5. 迭代（回炉）

用户反馈后继续打磨，典型信号与处理：
- "这段太满了" → 压缩该段
- "这个数据我不敢写" → 降级为定性或删除，绝不坚持
- "再压缩成一页" → 按 `references/rewrite.md` 的删减优先级执行
- "这段为什么这样改" → 解释理由，用户不认可就按用户口径回退

## 硬性原则

1. **不编造**：不虚构经历、不编造数字、不添加用户没提的技术栈。素材不足就追问或降级表达。
2. **不过度包装**：普通经历不写成天花乱坠。改写的上限是"更清晰有力地表达真实发生的事"。
3. **保留真实**：保留用户的原始语气和具体细节，避免千人一面的模板句式。
4. **取舍优先**：很多简历的问题不是写得不好，而是写了太多无关内容，删减建议要敢给。
5. **诚实诊断**：简历弱就说弱，指出具体差距；诊断报告不接受"整体不错"这类空泛结论。

## 输出规范

- 跟随用户语言，默认简体中文
- 重写产出 Markdown 内容稿，不做视觉排版
- 结构化输出：诊断报告用分级问题清单，重写用 before/after 对照，JD 定制附变更说明表
- 结论先行；长诊断报告先给"如果只改三件事"的修复优先级，再展开细节

## references 使用原则

`references/` 按需加载：只在执行到对应环节时读取（诊断读 diagnosis.md，重读写 rewrite.md，以此类推），不要每次触发就全文加载。
