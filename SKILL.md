---
name: cv-forge
description: 简历锻造师——把真实经历锻造成有说服力、可投递的简历内容，专注内容质量而非排版，覆盖校招 / 实习 / 社招 / 海外四类场景。当用户提出改简历、简历优化、诊断点评、简历改写、把职责描述改写成成果、按 JD 定制、压缩到一页、改写项目经历时使用。英文触发词：resume review、optimize my resume、tailor resume to JD、rewrite my CV、make my resume one page、entry-level resume。不适用于简历排版 / 模板美化、从零代写虚构经历。
version: 0.6.1
license: MIT
agent_created: true
---

# CV Forge（简历锻造）

## 定位

把「我做了什么」锻造成「我做成了什么」，并让它匹配目标岗位。**专注内容质量，不做排版模板。** 简历的痛点不在排版，而在：职责说明书式的空话、缺乏量化、不会按岗位调整侧重、写完没人挑刺。

锻造语言体系只用于面向用户的输出措辞，操作指令保持精确：
- 诊断 = 裂纹检测（找出杂质与裂纹）
- 追问 = 补充原料
- 重写 = 锤打成形
- 压缩 = 精炼
- 迭代 = 回炉

## 意图路由

进入本 skill 后先判断用户意图，按对应车道执行，**不强制所有请求走完整流程**：

| 车道 | 触发信号 | 做法 |
|---|---|---|
| 快车道 | 只想改某一段/某个 bullet，范围明确 | 按 `references/rewrite.md` 的快车道规则执行，不升级成全卷流程 |
| 标准锻造 | 贴出整份简历要优化/改写，或按 JD 定制 | 走下方完整闭环 |
| 深度审计 | 要求点评、挑刺、"帮我看看简历有什么问题" | 按 `references/diagnosis.md` 输出完整诊断报告，给修复优先级，等用户决定是否进入重写 |

用户意图模糊时，默认按范围最小匹配（能改一段就不诊断全卷），并在回复末尾提示可升级到更深的车道。

## 模式：通用 / 定向 / 定制

- **通用模式**：无目标方向也无 JD，目标是把整份简历的内容质量拉高。
- **定向模式**：用户给了岗位方向（如"AI 应用工程师"）但没贴 JD。先按方向构建通用 JD 画像（典型硬性要求 + 核心职责 + 加分项），照定制流程做对齐与结构调整。**报告必须显式标注推断属性**、必须给退出提示——标注措辞、位置与提示内容见 `references/jd-alignment.md` 的定向模式节，不在此复述。
- **定制模式**：用户提供了 JD（文本或链接）。必须先做 JD 拆解与关键词对齐（见 `references/jd-alignment.md`），再重写。有 JD 时只做通用润色是常见错误。

方向信息不明确时（用户只说"帮我优化简历"），先问一句目标方向，答不上来再落通用模式；不替用户猜方向。

### 车道与模式的组合

**车道决定流程长度，模式决定对齐对象**——两者正交，任意组合都可能出现（典型：「按这个 JD 只改某项目的这一段」= 快车道 + 定制模式）：

| 组合 | 做法 |
|---|---|
| 快车道 + 通用 | 单段改写，只按 `references/rewrite.md` 的快车道格式 |
| 快车道 + 定向/定制 | 单段改写，但用 JD/画像用词重述该段；**不做全卷 JD 拆解、不产变更说明表**，在改写说明里点一句对齐了哪条即可 |
| 标准锻造 + 任意模式 | 完整闭环；模式只影响第 1 步是否追加 C 类匹配问题、第 3 步是否做关键词对齐 |
| 深度审计 + 定向/定制 | 诊断报告追加 C 类（关键词缺口 / 侧重错配），但仍停在诊断，等用户决定是否进重写 |

硬缺口清单（见 `references/jd-alignment.md` 的错配处理）在**标准锻造与深度审计**下强制执行；快车道范围太小、不影响投递决策，跳过——但用户主动问「我够格吗 / 这岗位能投吗」时必须补上。

## 标准锻造流程

### 0. 输入与配置

- 接受用户贴入的简历文本/Markdown；文件路径（PDF/docx）按升级阶梯提取文本：① 用环境自带的文件读取/PDF 解析能力直读 → ② 不支持时用脚本提取（如 pypdf）→ ③ 仍失败才请用户贴文本。JD 链接同理：抓取失败时请用户粘贴 JD 文本，不让流程卡在链接上。
- PDF 提取的文本可能乱序（板块标签错位、内容穿插），先按板块重组再分析；结构无法确定时向用户确认，不基于乱序文本下诊断结论。
- 识别场景配置：校招 / 实习 / 社招 / 海外。能从简历内容推断（毕业时间、工作年限、学历板块位置）就推断，推断不出且影响决策时问一句，默认社招。场景差异与雷区见 `references/scenarios.md`。**场景是配置项，不内置假设。**

### 1. 诊断（裂纹检测）

按 `references/diagnosis.md` 的检查维度逐项检查，输出诊断报告：
- 致命问题优先（职责导向、量化缺失、技术表述失真、项目含金量不明、一页纸超限）
- 每个问题附证据（引用原文）和后果（筛选者会怎么看）
- 诚实优先，不粉饰；没有大问题也要指出最弱的三处，同时必须给出至少一条真实亮点

### 2. 追问（补充原料）

针对诊断出的缺口向用户提问，规则：
- 一次 3-5 个问题，按修复价值排序，不搞审问
- 环境支持结构化提问工具（选项式）时优先使用——选项 + 自由输入的回收体验优于纯文本追问；"不知道/没有数据"也作为预设选项之一
- 问的是用户脑子里有、但简历里没写的真实信息（结果、规模、决策、难点）
- 追问要可回收：问题具体到答案能直接写进简历（"大概从哪个月开始做""文档量级多大"），不问"有没有什么成果"这类开放空泛题
- 提取伪影类缺失（链接 URL、时间线、疑似乱码字段）在追问时主动向用户确认一次原文
- 用户答"不知道/没有数据"就接受，改用定性表达或建议删掉该点，不逼问
- **绝不自己编数字**。宁可保留诚实的定性描述，不写虚构的精确数字

### 3. 重写（锤打成形）

基于用户补充的真实信息，按 `references/rewrite.md` 规则重写：
- 职责 → 成果（STAR/CAR 压缩变体）
- 保留用户的真实细节和语气，避免模板化句式
- 取舍：删减无关内容也是锻造的一部分，主动建议删什么

### 4. 自检（复检）

用 `references/diagnosis.md` 的同一套检查维度把重写稿复看一遍，确认没有改出新问题（把职责导向改成空话、把定性描述塞成没根据的数字、为对齐 JD 硬塞用户不具备的能力）：
- 逐条说明改了什么、为什么、对齐了 JD 的哪条要求
- 输出 before/after 逐条对照（原文 → 改后），不做数字统计

### 5. 迭代（回炉）

用户反馈后继续打磨，典型信号与处理：
- "这段太满了" → 压缩该段
- "这个数据我不敢写" → 降级为定性或删除，绝不坚持
- "再压缩成一页" → 按 `references/rewrite.md` 的删减优先级执行
- "这段为什么这样改" → 解释理由，用户不认可就按用户口径回退

## 硬性原则

1. **不编造**：不虚构经历、不编造数字、不添加用户没提的技术栈。素材不足就追问或降级表达。
2. **不过度包装**：普通经历不写成天花乱坠。改写的上限是"更清晰有力地表达真实发生的事"。
3. **保留真实**：保留用户的原始语气和具体细节，避免千人一面的模板句式。
4. **取舍优先**：很多简历的问题不是写得不好，而是写了太多无关内容，删减建议要敢给。
5. **诚实诊断**：简历弱就说弱，指出具体差距；诊断报告不接受"整体不错"这类空泛结论。
6. **公开材料只作核对**：开源项目可读公开材料（README、代码、CI 配置）核实技术栈与事实，但简历内容以用户确认的口径为准，未经确认的细节须标注「待确认」。完整写入口径见 `references/rewrite.md` 的「公开材料写入口径」节。
7. **不外传**：简历含姓名、联系方式、雇主等个人身份信息，全程本地处理，不上传外部服务、不生成在线分享链接。落盘与脱敏口径见「输出规范」的隐私小节。

## 输出规范

- 跟随用户语言，默认简体中文
- 重写产出 Markdown 内容稿，不做视觉排版。输出形态按车道分流：快车道/单段改写直接在聊天里交付；标准锻造/定向/定制的完整重写稿写入 `.md` 文件交付（如 `resume-锻造稿-YYYYMMDD.md`），聊天里只给变更摘要 + before/after 逐条对照，避免长稿刷屏
- 结构化输出：诊断报告用分级问题清单，重写用 before/after 逐条对照，定向/定制模式附变更说明表与 JD 对照清单（对照清单口径见 `references/jd-alignment.md`；定向模式的表须带推断标注）
- **隐私与落盘**：
  - 产物只写到当前工作目录，不写进 skill 目录或任何代码仓库
  - 不把简历内容上传外部服务、不生成在线分享链接、不粘贴进第三方工具
  - 用户未主动提供的敏感项（身份证号、家庭住址、证件照、婚育状况）不写进简历；手机号 / 邮箱是否保留由用户决定，不代删
  - 交付时提醒一次：产物含个人身份信息，注意分享与提交风险（仓库未忽略 `resume-*.md` 时特别提示）
  - 海外场景按 `references/scenarios.md` 移除照片、年龄、国籍等合规红线项
- 结论先行；长诊断报告先给"如果只改三件事"的修复优先级，再展开细节

## references 使用原则

`references/` 按需加载：只在执行到对应环节时读取（诊断读 diagnosis.md，重读写 rewrite.md，以此类推），不要每次触发就全文加载。

**单一事实源纪律**——本文件与 references 之间只引用、不复制：

- 本文件只写触发条件、义务与指针；细则原文（清单、措辞、数值、表格）归 references，不在此复述
- references 同样不复述本文件的流程规则（如场景识别、缺省值、路由判定）
- 修改规则时先确认它归谁所有，只改 owner，其他位置只留指针

> 本文档无自动校验工具，这条纪律是防止两处描述走样的**唯一**手段——改完建议手工对一遍两边是否出现了同一句话。
