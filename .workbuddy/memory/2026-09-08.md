# 2026-09-08

- cv-forge 立项落地：定位为**发布给大众的通用简历锻造 skill**（非个人 skill），中文校招/社招场景做成可配置项而非内置。旧的 job-hunt-assistant 已被用户删除，cv-forge 从零开始。
- 完成初版结构：SKILL.md（意图路由三车道：快车道/标准锻造/深度审计 + 诊断→追问→重写→自检→迭代闭环 + 五条硬性原则）+ references/ 四件套（diagnosis.md 诊断rubric含复检指标、rewrite.md 改写规则含删减优先级、jd-alignment.md JD定制、scenarios.md 场景差异与雷区）+ README 重写。
- 设计共识（来自用户的产品思考）：胜负手在内容方法论不在功能数量；锻造隐喻只用于用户可见措辞、操作指令保持精确；不编造数据是最高原则；意图路由（用户只想改一段时不强制走全流程）是隐形 P0。
- 待办：真实简历数据实测迭代；考虑 P1 功能（项目含金量评估、多版本管理）与 P2（HR 视角点评、自我介绍稿）。
- 16:11 按用户 review 意见落地 5 处优化：快车道"用户拒绝追问"降级处理（SKILL.md + rewrite.md 同步）、诊断亮点不足时的诚实兜底（SKILL.md + diagnosis.md 同步）、JD/文件链接抓取失败引导贴文本防卡流程、长报告"先三件事优先级再细节"写入输出规范、新增"references 按需加载"原则节。
- 16:27 用户开始真实简历实测，问如何评估效果。新建 EVALUATION.md 评估框架：输出质量 4 测试（复检指标 / 真实性审计 / 面试压力测试 / 盲测 A/B）+ 过程合规 4 检查（意图路由 / 追问纪律 / 原则违反 / 场景识别）+ 4 画像样本覆盖矩阵。核心原则：压力测试和盲测必须用无上下文新对话做，防自我偏袒。
- 16:33 用户贴回实测记录（req-1788855807823002，glm-5.3 新对话，深度审计车道，bibilabu.pdf 简历）。评估结论：过程合规全过（路由正确、按需加载、场景推断社招、追问 3 个≤5、无编造、诚实亮点、注水"企业级"正确标记），诊断质量高（抓到 3 年经历真空、量化 0%、三线并行）。发现缺陷：PDF 处理路径含糊（Read 工具 1007 失败后 AI 自行建 venv+装 pypdf），已修复为三级升级阶梯（直读→脚本→请用户贴文本），repo 与已安装副本（~/.workbuddy/skills/cv-forge）已同步。注意：skill 已被用户安装到用户级目录，repo 改动需手动同步。
- 待测车道：标准锻造闭环（答追问→重写→复检）、快车道、JD 定制。
- 16:39 用户从实测记录提炼 4 条打磨点，我又挖出 3 条，共 7 处落地：年龄等个人信息处理进 scenarios.md 社招节、🟡 明显类超 4 条压缩为汇总表、场景判断显式声明+依据进 diagnosis.md 报告开头、复检指标基线表留底（即使只诊断不重写）、追问可回收原则（问题具体到答案能直接进简历）、提取伪影字段追问时确认原文、PDF 提取乱序先重组再诊断。repo→已安装副本已同步（cp + diff 校验 SYNC OK）。
- 17:25 按用户建议新增硬性原则 #6「公开材料只作核对」：开源项目可读公开 README 核实技术栈与可验证事实，但写入简历前必须经用户确认，演示数据/内部实现细节未经确认不写入，定位与成果以用户口径为准。已同步安装副本。
- 17:44 用户又跑了一轮标准锻造实测（重写车道首过：简历不注水、面试口径单独给）。按反馈落地 3 处：#6 细化为「结构性数字（star/技术选型/架构）确认即写 vs 使用数据（用户量等）默认不写」二分口径；diagnosis.md 复检指标加「未达标如实标注受约束原因，诚实优先于指标好看」；scenarios.md 新增「空窗期处理」节（简历不注水 + 面试话术单独给 + 两端分离）。三个文件已同步安装副本（diff 全过）。
- 18:00 收束：核心能力验证完成（深度审计✓ 标准锻造✓，快车道/JD定制未实测）。落地最后两点：jd-alignment.md 新增「错配处理」节（硬缺口清单+投递风险先行，用户确认后才给定稿）；#6 重构为精简版+指针，详细口径（结构性数字/使用数据/repo·CI推断标注待确认）下沉到 rewrite.md 新增「公开材料的写入口径」节。SKILL.md 保持 lean 的结构原则自此确立：原则区一句话+references 承载操作细节。全部同步安装副本。
- 18:06 README.md 按用户定稿重写：新增「它解决什么问题」「不做什么」、核心能力表格、4 条真实触发语快速开始，补「主文件轻量、细则按需加载」。已核对 references/ 文件名与文档一致。README 不需要同步安装副本（skill 安装只含 SKILL.md + references/）。

## 今日总结
cv-forge 一天内完成：定位拍板（通用大众 skill、场景可配置）→ 骨架（SKILL.md + references 四件套）→ EVALUATION.md 评估框架 → 两轮真实数据实测（bibilabu.pdf，glm-5.3 新对话）+ 一轮重写实测 → 累计 ~17 处指令打磨。所有修改均同步 ~/.workbuddy/skills/cv-forge（注意：repo 是 source of truth，改动需手动同步安装副本）。
